# GoDrive Frontend - Environment Variables
# Create .env in frontend/ and set:

# Backend API base URL (empty when using Vite proxy in dev)
# In production behind nginx: leave empty so /api goes to same origin
VITE_API_URL=
